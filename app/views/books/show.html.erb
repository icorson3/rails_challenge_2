<%= @book.title %>

<%= @book.reviews.each do |review|%>
  <%= review.user.name %>
  <%= review.body %>
  <%= review.rating %>
<% end %>

<h2>Average Rating<h2>
<%= Review.average_rating(@book.id)%>



