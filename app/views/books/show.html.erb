<h2> Book details </h2>
  <ul>
  <%= @book.title %>
  <%= @book.reviews.each do |review| %>
   <%= review.body %>
   <%= review.rating %>
   <%= review.user_id %>
   <% end %>

   <h2> Average review </h2>
     <%= @average %>
